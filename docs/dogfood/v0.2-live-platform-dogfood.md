# v0.2 Live Platform Dogfood

Date: 2026-07-02

## Environment

- Branch: main
- Commit: bdd8c45
- GitHub credentials: present via local `gh` keyring token, injected only into the command environment
- Feishu credentials: missing
- Offline verification: pass

## Platform Doctor

- Result: partial
- Evidence: `devflow doctor --platform github` passes when `GITHUB_TOKEN` is populated from `gh auth token` for the command; `devflow doctor --platform feishu` reports missing `FEISHU_APP_ID` and `FEISHU_APP_SECRET` without printing secret values.

## GitHub Issue Intake

- Source: `jesseedcp/devflow-agent#25`
- Demand ID: `dogfood-coupon-eligibility-platform-intake`
- Result: pass
- Evidence: intake created `.devflow-live-dogfood/github-issue/.devflow/demands/dogfood-coupon-eligibility-platform-intake`; `intake.md` contains `Source: github_issue` plus the dogfood issue title/body, `requirements.md` was generated, and status is `requirements_review`.

## GitHub Issue Sync

- Target: `jesseedcp/devflow-agent#25`
- Result: pass
- Evidence: sync wrote a local `platform.sync_posted` event, posted one GitHub comment containing the Devflow sync marker, and a second sync reused the same marker comment instead of creating a duplicate.

## Feishu Doc Intake

- Source: not run
- Demand ID: not run
- Result: blocked
- Evidence: no `FEISHU_APP_ID`/`FEISHU_APP_SECRET` and no `DEVFLOW_DOGFOOD_FEISHU_DOC` asset available.

## Feishu Bitable Pool

- Target: not run
- Result: blocked
- Evidence: no Feishu credentials and no `DEVFLOW_DOGFOOD_BITABLE_APP`/`DEVFLOW_DOGFOOD_BITABLE_TABLE` assets available.

## Feishu Bitable Intake

- Source: not run
- Demand ID: not run
- Result: blocked
- Evidence: depends on Feishu credentials and a disposable Bitable record; none provided.

## Feishu Bitable Sync

- Target: not run
- Result: blocked
- Evidence: depends on Bitable intake, which is blocked on missing credentials/assets.

## Final Verdict

Blocked for full live dogfood; partial pass. GitHub Issue live intake and sync worked against disposable issue `jesseedcp/devflow-agent#25` using the local GitHub CLI keyring token without recording token values. Feishu live dogfood could not run because Feishu credentials and disposable Doc/Bitable assets are not available in this environment. Offline release readiness remains healthy and credential-free. No secrets were recorded.

## Follow-Ups

- Provide Feishu live credentials (`FEISHU_APP_ID`, `FEISHU_APP_SECRET`) and disposable assets (`DEVFLOW_DOGFOOD_FEISHU_DOC`, `DEVFLOW_DOGFOOD_BITABLE_APP`/`_TABLE`/`_RECORD`) to complete Feishu Doc/Bitable live dogfood.
- `devflow evidence add` currently requires `verification` state; the dogfood plan tried to add evidence while the intake workspace was still in `requirements_review`. Either update the dogfood plan or add a supported progress/evidence path for requirements-review sync proof.
- Release-readiness Feishu/URL fake-server smokes intermittently hit a Windows HttpListener `connection refused` race; the run passes on retry. Consider hardening the smoke harness startup wait to remove the flake.
