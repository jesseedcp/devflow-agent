# v1.1 Runtime Provider Dogfood

## Verdict

PASS

The v1.1 hardening fixes all three root causes from the failed v1.0.1 Ark glm-5.2 dogfood: plan stages now allow safe read/search tools, `plan.steps` accepts child-heading content, and the OpenAI-compatible streaming tool-call path drives tool execution to completion. The implementation stage produced correct, tested code without manual edits. The first implementation run hit the 20-iteration cap after writing code; a second autonomous run verified the existing work and completed the stage.

## Environment

- Devflow commit: b7f919d (branch v11-runtime-tool-calling-hardening)
- Provider protocol: openai-compatible
- Provider host: ark.cn-beijing.volces.com
- Model: glm-5.2
- API key: present, redacted
- Dogfood repo: D:\Users\dd\Desktop\agent学习\weather-agent-runtime-dogfood-v11
- Dogfood baseline: 5486bb2 (weather-agent baseline for runtime dogfood)

## Runtime Stage Results

| Stage | Command | Exit | Duration | Artifact | Manual edit? | Result |
| --- | --- | ---: | ---: | --- | --- | --- |
| plan | `devflow run --stage plan --demand weather-tour-recommendation` | 0 | 254s | plan.md | no | plan.steps PASS, 0 blockers |
| implementation (1st) | `devflow run --stage implementation --demand ... --permission-mode bypassPermissions` | 1 | 221s | code written, progress.md not saved | no | max iterations (20); wrote tools.go, prompt.go, main_test.go; 21 tool calls |
| implementation (2nd) | `devflow run --stage implementation --demand ... --permission-mode bypassPermissions` | 0 | 81s | progress.md | no | quality gate passed; state -> mr_review |

## Evidence

- Plan strict evaluation: warning (blockers=0, warnings=2 — codemap_reference, context_grounding). The `plan.steps` blocker from v1.0.1 is resolved.
- Implementation review: needs_scope_review (warning, not blocker).
- Tests: 4/4 PASS — TestIsRainyWeather, TestEmptyCityReturnsCityRequired, TestGetAttractionMissingWeather, TestSunnyWeatherNotRainy.
- Acceptance evidence: link evidence to test output. API evidence not applicable — the weather demo is a CLI agent, not an HTTP API.
- Final demand state: mr_review (implementation completed).

## Code Produced (no manual edits)

- `tools.go`: added `isRainyWeather` function; `getAttraction` routes to indoor query on rain; `CITY_REQUIRED` error code for empty city.
- `prompt.go`: updated `AGENT_SYSTEM_PROMPT` with rainy-weather rules and `CITY_REQUIRED` documentation.
- `main_test.go`: 4 test functions covering rainy detection, empty-city error code, missing-weather edge case, and sunny non-misclassification.

## Regressions Checked

- Safe read/search tools allowed in plan: YES — plan.md shows the model used Glob, Grep, and ReadFile during planning.
- `plan.steps` child headings accepted: YES — `plan.steps` PASSES strict evaluation (was a blocker in v1.0.1).
- Tool results observed by next turn: YES — the model made progressive tool calls (ReadFile -> EditFile -> Bash/test), confirming tool results feed back into the next LLM turn.
- Max-iteration diagnostics: YES — first implementation run reported `tool calls=21 last tools=Bash,Bash,EditFile,EditFile,Bash` (was a bare "agent error" in v1.0.1).

## Deviations

- Implementation required 2 autonomous runs. The first run wrote correct code and tests but hit the 20-iteration cap without generating a final text response. The second run read the existing code, ran tests (pass), and wrote the progress.md summary, completing the stage. No manual edits were made to any file at any point.
- Strict evaluations have advisory warnings (0 blockers): `plan.codemap_reference` (codemap had no likely-impacted files for the small demo), `plan.context_grounding`, and `implementation.review` (scope review — prompt.go was edited but not declared in scope). These are pre-existing evaluation checks, not related to the v1.1 hardening.

## Secrets Check

No API keys, bearer tokens, cookies, or provider response secrets are included.