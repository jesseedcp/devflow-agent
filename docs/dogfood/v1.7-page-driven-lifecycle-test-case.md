# v1.7 Page-Driven Lifecycle Test Case

## Purpose

Verify that a real operator can use the Web UI, backed by MySQL and the platform server, to create a workspace, create a demand, inspect artifacts, confirm a manual gate, and understand which actions are not yet page-backed.

## Environment

- MySQL reachable at `127.0.0.1:3316`.
- Backend:
  `devflow server --addr 127.0.0.1:8088 --root <repo-or-demo-root>`
- Frontend:
  `VITE_DEVFLOW_API_MODE=http`
  `VITE_DEVFLOW_API_BASE=http://127.0.0.1:8088/api`
  `npm run dev -- --host 127.0.0.1 --port 5183`

## Test Data

- Workspace ID: `ui-dogfood`
- Workspace name: `UI Dogfood`
- Demand key: `coupon-ui-dogfood`
- Demand title: `Coupon UI dogfood`
- Demand description: `Inactive users must be blocked from claiming coupons.`

## Steps

1. Open `http://127.0.0.1:5183`.
2. Go to Workspaces.
3. Click `创建工作区`.
4. Fill workspace ID, name, and artifact root.
5. Submit and verify the new workspace appears.
6. Open the workspace demands page.
7. Click `新建需求`.
8. Fill demand key, title, and description.
9. Submit and verify the demand detail page opens.
10. Verify artifact tabs show generated demand artifacts.
11. Verify quality and next actions are visible.
12. Click `通过下一步`.
13. Verify the state changes from `requirements_review` to `plan_drafting`.
14. Verify runtime-only actions are disabled with a clear reason.
15. Go to Release and Wiki pages.
16. Verify unbacked actions are disabled with clear copy instead of throwing.
17. Open Audit and verify workspace/demand actions were recorded.

## Pass Criteria

- No visible button throws or silently does nothing.
- Workspace creation works against the real backend.
- Demand creation works against the real backend.
- Demand detail is populated from backend lifecycle data.
- `通过下一步` advances a supported manual gate.
- Unsupported actions are disabled and explain why.
- Audit contains records for platform actions.

## Report Template

- Result: PASS / FAIL
- Browser:
- Backend command:
- Frontend command:
- Workspace:
- Demand:
- Screenshots:
- Failures:
- Follow-ups:

## Dogfood Result

- Result: PASS (API lifecycle smoke + browser page flow)
- Browser: in-app Chromium browser against the Vite dev server
- Backend command: `dist/devflow-platform.exe server --addr 127.0.0.1:8088 --root D:\Users\dd\Desktop\agent学习\mewcode-golang\devflow-agent\.worktrees\v17-page-driven-lifecycle` with `DEVFLOW_DATABASE_DSN=devflow:devflow@tcp(127.0.0.1:3316)/devflow?parseTime=true`, `DEVFLOW_DEV_USER_EMAIL=devlocal@example.com`, `DEVFLOW_DEV_USER_ROLE=Admin`
- Frontend command: `VITE_DEVFLOW_API_MODE=http`, `VITE_DEVFLOW_API_BASE=http://127.0.0.1:8088/api`, `npm run dev -- --host 127.0.0.1 --port 5183`
- Workspace: `ui-dogfood-20260708`
- Demand: `coupon-ui-dogfood-20260708`
- Screenshots:
  - `C:\Users\dd\AppData\Local\Temp\devflow-v17-page-dogfood\workspaces.png`
  - `C:\Users\dd\AppData\Local\Temp\devflow-v17-page-dogfood\demand-detail-confirmed.png`
  - `C:\Users\dd\AppData\Local\Temp\devflow-v17-page-dogfood\release-disabled.png`
  - `C:\Users\dd\AppData\Local\Temp\devflow-v17-page-dogfood\wiki-candidates-disabled.png`
  - `C:\Users\dd\AppData\Local\Temp\devflow-v17-page-dogfood\audit.png`
- Failures: none
- Console errors: 0
- Follow-ups: none required for the v1.7 page-driven lifecycle release gate

Verified browser steps:

1. Opened `http://127.0.0.1:5183` in real HTTP mode; header showed `devlocal@example.com` / `Admin` and no mock tag.
2. Created workspace `ui-dogfood-20260708` with a real artifact root.
3. Opened the workspace demand list.
4. Created demand `coupon-ui-dogfood-20260708`.
5. Verified the demand detail page opened and showed 19/19 generated artifacts.
6. Verified quality table and next actions were visible.
7. Verified evidence recording was disabled outside `verification` with the message `只有 verification 阶段可以记录验收证据。`
8. Clicked `通过下一步`; state advanced from `requirements_review` to `plan_drafting` and the page rendered `✓ 已确认 · 状态推进到 plan_drafting`.
9. Verified runtime-only continuation was disabled with the message `当前阶段需要 CLI/Agent 产物，暂不支持页面直接推进。`
10. Opened the Release page and verified rollback plus observation refresh were disabled with explicit backend-unavailable reasons.
11. Opened Wiki Library and Wiki Candidates; both explained that HTTP backend wiki routes are not open yet.
12. Opened Audit and verified `configure_workspace`, `create_demand`, and `confirm_gate` entries.

The automated API smoke (`scripts/platform-page-lifecycle-smoke.ps1`) also passed against MySQL on `127.0.0.1:3316`.
